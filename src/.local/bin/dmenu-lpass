#!/bin/bash

username=`envchain ytet5uy4/lastpass printenv LPASS_USERNAME`

lpass status -q || termite -e "lpass login $username"

declare name=`
  lpass ls | \
    sed -E 's/ \[id: ([0-9]+)\]//' | \
    dmenu-wrapper -p Lastpass
`

[[ $name ]] && lpass show --password -c $name
