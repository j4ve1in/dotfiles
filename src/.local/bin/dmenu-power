#!/bin/zsh

power=( poweroff reboot suspend lock )

input=`<<< ${(F)power} | dmenu-wrapper -p Power`

case $input in
  poweroff) systemctl poweroff ;;
  reboot) systemctl reboot ;;
  suspend) systemctl suspend ;;
  lock) light-locker-command -l ;;
  *) ;;
esac
