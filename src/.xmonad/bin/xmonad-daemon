#!/bin/zsh

while (( $# > 0 )); do
  case "$1" in
    tmux)
      tmux new-session -s 0 -d
      shift
      ;;
    compton)
      compton -b
      shift
      ;;
    mopidy)
      mopidy &
      shift
      ;;
    *) shift ;;
  esac
done
