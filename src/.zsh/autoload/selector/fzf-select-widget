fzf_widgets=(
  `ls .zsh/autoload/selector/ | \
    sed -e 's/fzf-//g' -e 's/-widget//g' | \
    egrep -v '^(select|selector-init)$'`
)
widget=$(echo ${(F)fzf_widgets} | $SELECTOR)
[ -z "$widget" ] && return 1
fzf-${widget}-widget

# vim: ft=zsh sw=2 ts=2 et
