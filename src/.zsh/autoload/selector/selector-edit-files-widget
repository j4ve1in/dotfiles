private -a file=($(find . -type f | sed 's|\./||g' | $SELECTOR))
[[ -z $file ]] && return 1
for file in $file; do
  [[ ! -f $file ]] && return 1
done
BUFFER="$EDITOR $file"
CURSOR=$#BUFFER
zle accept-line
zle -R -c

# vim: ft=zsh sw=2 ts=2 et
