FILES=($(find * -type f | $SELECTOR))
[ -z "$FILES" ] && return 1
for file in ${FILES[@]}; do
  [ ! -f "$file" ] && return 1
done
if zle; then
  BUFFER="$EDITOR ${FILES[@]}"
  CURSOR=$#BUFFER
  zle accept-line
  zle -R -c
else
  print -z -f '%s' "$EDITOR ${FILES[@]}"
fi
# vim: ft=zsh sw=2 ts=2 et
