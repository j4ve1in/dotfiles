FZF_WIDGETS=(
  'edit-files'
  'cd-dir'
  'cd-ghq-repository'
  'cdr'
  'excute-history'
  'git-add'
  'checkout-git-branch'
  'lpass-bookmark'
)
widget=$(echo $FZF_WIDGETS | tr ' ' '\n' | $SELECTOR)
[ -z "$widget" ] && return 1
fzf-${widget}-widget

# vim: ft=zsh sw=2 ts=2 et
