DIR=$(find * -type d | $SELECTOR)
if [ -d "$DIR" ]; then
  if zle; then
    BUFFER="cd $DIR"
    CURSOR=$#BUFFER
    zle accept-line
    zle -R -c
  else
    print -z -f '%s' "cd $DIR"
  fi
fi
# vim: ft=zsh sw=2 ts=2 et
