#!/bin/zsh

for dir in {Documents,Music,Pictures,Videos,Source_code}; do
  if [ ! -d "$dir" ] && [ ! -L "$dir" ]; then
    ln -s "$HOME/.local/share/dropbox/$dir" "$dir"
  else
    echo "$dir already exists."
  fi
done
