# Plugin
## Initialize tpm
set -g @tpm_plugins '       \
  tmux-plugins/tpm          \
  tmux-plugins/tmux-yank    \
  tmux-plugins/tmux-open    \
  tmux-plugins/tmux-copycat \
'
run-shell 'tpm-init'

## Initialize Powerline
run-shell 'powerline-init'

# Prefix
set-option -g prefix C-t
unbind-key C-b
bind C-t send-prefix
## Command Sequence for Nested Tmux Sessions
bind-key a send-prefix

# Statusline
run-shell 'set-tmux-statusline'

# Keybind
## Vim-like
bind-key h select-pane -L
bind-key j select-pane -D
bind-key k select-pane -U
bind-key l select-pane -R
bind-key -r C-h select-window -t :-
bind-key -r C-l select-window -t :+
bind-key -r H resize-pane -L 5
bind-key -r J resize-pane -D 5
bind-key -r K resize-pane -U 5
bind-key -r L resize-pane -R 5
set-window-option -g mode-keys vi
bind-key -t vi-copy v begin-selection

## Launch
bind-key e new-window "vim" \; display "Editor"
bind-key E split-window -h -v 'vim' \; display "Editor"
bind-key F split-window -h -v 'feednix' \; display "Feedly"
bind-key S command-prompt -p "ssh:" "split-window -h -v 'ssh -t %1'"
bind-key T command-prompt -p "trans:" "split-window -h -v 'trans %% | less'"
bind-key m command-prompt -p "man:" "split-window -h -v 'exec man %%'"
bind-key M command-prompt -p "jman:" \
  "split-window -h -v 'LANG=ja_JP.UTF-8 exec man %%'"

# Mouse
run-shell 'set-tmux-mouse-option'

# monitor-activity
set-window-option -g monitor-activity on
set-window-option -g window-status-activity-attr none

# Others
set-option -sg escape-time 1
set-option -g status-interval 1
set-option -g visual-activity off
set-option -g display-time 1000
set-option -g terminal-overrides 'xterm*:smcup@:rmcup@'
bind-key [ copy-mode \; display "Copy mode"
bind-key Escape copy-mode \; display "Copy mode"
bind-key r source-file ~/.tmux.conf \; display "Configuration Reloaded"
