#!/bin/zsh

if [[ -d ${TMUX_PLUGIN_MANAGER_PATH:h} ]]; then
  echo 'Already installed "tpm"'
else
  echo 'Installed "tpm"'
  git clone --depth 1 https://github.com/tmux-plugins/tpm ${TMUX_PLUGIN_MANAGER_PATH:h}
  echo
  echo 'Done, press ENTER to continue.'
  tpm-init
fi
