#!/bin/bash

if type powerline >/dev/null 2>&1;then
  tmux run-shell 'powerline-daemon -q'
  tmux if-shell 'env "$POWERLINE_CONFIG_COMMAND" tmux setup' '' \
                'run-shell "powerline-config tmux setup"'
fi
