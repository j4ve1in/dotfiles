#!/bin/bash

tmux if-shell 'check-powerline' " \
  run-shell 'powerline-daemon -q'; \
  if-shell 'env \"$POWERLINE_CONFIG_COMMAND\" tmux setup' '' \
    'run-shell \"powerline-config tmux setup\"' \
"
