[color]
    ui = true

[color "diff"]
    meta = white bold
    frag = cyan
    old = red
    new = green

[core]
    attributesfile = ~/.gitattributes.global
    excludesfile = ~/.gitignore.global
    editor = vim
    pager = less
    quotepath = off

[include]
    path = ~/.gitconfig.aliases
    path = ~/.gitconfig.proxy
    path = ~/.gitconfig.local.d/userinfo.gitconfig
    path = ~/.gitconfig.local.d/credential.gitconfig
    path = ~/.gitconfig.local.d/sshinsteadof.gitconfig
    path = ~/.gitconfig.local.d/httpspushinsteadof.gitconfig

[push]
    default = simple
