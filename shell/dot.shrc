# Load ~/.shrc.d/*.rc.sh
SHRC=(
  "aliases"
  "base"
  "function"
  "last"
)
for file in ${SHRC[@]}; do
  file=~/.shrc.d/${file}.rc.sh
  [ -r "$file" ] && [ -f "$file" ] && source "$file"
done; unset -v SHRC file
